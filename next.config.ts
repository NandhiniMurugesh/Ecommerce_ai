/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['vootbuy.in', 'images.meesho.com', 'i.etsystatic.com','chasevalue.pk'], // add any other domains you're using
  },
};

module.exports = nextConfig;
